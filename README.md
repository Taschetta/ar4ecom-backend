# ar4ecom-backend

## Build Setup

```bash
# install dependencies
$ npm install

# serve with hot reload at localhost:3333
$ npm start

# linting and testing
$ npm run lint
$ npm run test
$ npm run test --workspaces
```